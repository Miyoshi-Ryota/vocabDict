{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AddWordToVocabularyListResponse",
  "description": "Response from adding a word to a vocabulary list.",
  "type": "object",
  "required": ["success"],
  "properties": {
    "success": {
      "type": "boolean",
      "description": "Whether the word was successfully added."
    },
    "data": {
      "type": "object",
      "description": "The word entry data if successfully added.",
      "properties": {
        "id": {
          "type": "string",
          "format": "uuid"
        },
        "word": {
          "type": "string"
        },
        "metadata": {
          "type": "object"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      }
    },
    "error": {
      "type": "string",
      "description": "Error message if the operation failed."
    }
  }
}