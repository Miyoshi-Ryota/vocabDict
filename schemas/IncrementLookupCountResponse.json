{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "IncrementLookupCountResponse",
  "description": "Response from incrementing the lookup count for a word.",
  "type": "object",
  "required": ["success"],
  "additionalProperties": false,
  "properties": {
    "success": {
      "type": "boolean",
      "description": "Whether the lookup count was successfully incremented."
    },
    "error": {
      "type": "string",
      "description": "Error message if the operation failed."
    }
  }
}
