{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "FetchLookupCountRequest",
  "description": "Request to fetch the lookup count for a specific word.",
  "type": "object",
  "required": ["action", "word"],
  "additionalProperties": false,
  "properties": {
    "action": {
      "type": "string",
      "const": "fetchLookupCount",
      "description": "The action to be performed. Must be 'fetchLookupCount'."
    },
    "word": {
      "type": "string",
      "minLength": 1,
      "description": "The word to get the lookup count for."
    }
  }
}
