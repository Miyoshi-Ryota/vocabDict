{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "GetPendingContextSearchRequest",
  "description": "Request to retrieve a pending word selection to search when opening popup.",
  "type": "object",
  "required": ["action"],
  "additionalProperties": false,
  "properties": {
    "action": { "type": "string", "const": "getPendingContextSearch" }
  }
}
