{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AddRecentSearchResponse",
  "description": "Response from adding a word to recent searches.",
  "type": "object",
  "required": ["success"],
  "additionalProperties": false,
  "properties": {
    "success": {
      "type": "boolean",
      "description": "Whether the word was successfully added to recent searches."
    },
    "error": {
      "type": "string",
      "description": "Error message if the operation failed."
    }
  }
}
