{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "FetchAllVocabularyListsRequest",
  "description": "Request to fetch all vocabulary lists.",
  "type": "object",
  "required": ["action"],
  "properties": {
    "action": {
      "type": "string",
      "const": "FETCH_ALL_VOCABULARY_LISTS",
      "description": "The action to be performed. Must be 'FETCH_ALL_VOCABULARY_LISTS'."
    }
  }
}
